<script>
$.getScript("_/js/loading_scripts/activejobtablescript.js");

</script>

<div class="Instructions">
  <h3>Active / Pending / Draft Job Posting</h3>
  <p>Edit Button: To edit a job that was posted earlier</p>
  <p>Cancel Button: You can cancel a job posting if the position is no longer required. Cancelled jobs can be viewed in Closed/Cancelled Jobs tab and they can be reinstated as a new job posting</p>
  <p>Pending Status: Job postings that need to be verified by your colleague/client. Pending jobs will not be matched with candidates until it is Active</p>
  <p>Active Status: Active job postings are matched with potential candidates. You can view the matching result in the Recruiter Dashboard</p>
  <p>Draft Status: Select this job posting status if you have not completed filling in the job detail.</p>
</div>

  <table id="joblistingtable" class=" table table-striped table-bordered table-hover">
    <thead>
      <tr>
         <th>ID</th>
         <th>Function</th>
         <th>Posted</th>
         <th>Status</th>
         <th></th>
         <th></th>
      </tr>
    </thead>
    <tbody id="aat" >
      <% @joblisting.each do |i| %>
      <% if i.job_status == 1 || i.job_status ==2 || i.job_status ==4 %>
      <tr>
        <td><%= i.id%></td>
        <td>
          <%= if i.job_title != nil 
                @titlemaster.get(i.job_title).title  end%> 
        </td>
        <td><%= i.job_posted.strftime("%d/%m/%Y") %></td> 
        <td>
          <%= if i.job_status == 0
               "Deleted"
             elsif i.job_status == 1
              "Pending"
             elsif i.job_status == 2
              "Active"
             elsif i.job_status == 3
               "Closed"
             elsif i.job_status == 4
               "Draft"
             else  "NA"
             end
          %>  
        </td> 
        <td>
          <div class="action-buttons">
            <a onclick="showjobdetail(<%=i.id%>)" class="red" href="javascript:void(0)"> 
              <i class="fa fa-pencil fa-lg"></i>
            </a>      
          </div><!-- This calls newjobdetail.erb -->
        </td>
        <td>
          <div class="action-buttons" >
            <a onclick="deljob(<%=i.id%>)" href="javascript:void(0)"> 
              <i class="fa fa-trash-o fa-lg"></i>
            </a>      
          </div>
        </td>
      </tr>
      <% end %>
      <% end %>
    </tbody>
  </table> <!--joblistingtable -->